# Decision Log

Record only decisions that affect architecture, scope, policy, data meaning, security, or acceptance.

| ID | Date | Decision | Reason | Alternatives considered | Consequences | Status |
|---|---|---|---|---|---|---|
| D-001 | Initial | Version 1 is monitoring/decision support with synthetic telemetry; no physical control. | Safety and unavailable authorized OT inputs. | Direct control in MVP. | Control interfaces remain read-only/adapted. | Accepted |
| D-002 | Initial | Stage (m), discharge (m³/s), and volume (m³) are distinct domain quantities. | Required for correct hydrometry and reporting. | Generic `water_value`. | Schemas and UI must carry explicit type/unit. | Accepted |
| D-003 | 2026-08-23 | Use a TypeScript pnpm modular monolith with Fastify, React/Vite, and PostgreSQL/PostGIS. See ADR-0001. | Smallest deployment that supports typed contracts, GIS, deterministic domain modules, and future extraction. | Microservices/broker; Next.js full stack; mixed Python/TypeScript; SQLite. | Domain/contracts remain transport-neutral; no distributed infrastructure without evidence. | Accepted |
| D-004 | 2026-08-23 | Use append-oriented observation revisions, effective-dated governance records, and immutable report snapshots. See ADR-0002. | Corrections and historical reports must be explainable and reproducible. | Mutable current rows; reports rebuilt from latest data. | Reads distinguish current vs as-of state and derived values carry coverage/quality. | Accepted |
| D-005 | 2026-08-23 | Simulator telemetry enters through an adapter and streams to clients by SSE; the MVP has no physical-control command path. See ADR-0003. | Demonstrates live/replay workflows while preserving OT/IT safety and vendor neutrality. | Initial MQTT broker; WebSockets; vendor/PLC integration. | Future device/control work requires separate authorized architecture. | Accepted |
| D-006 | 2026-08-23 | Model the nominal/accounting water network as an authoritative directed acyclic graph of junction nodes and section edges, separate from administrative territory hierarchy. See ADR-0004. | Supports branches, merges, deterministic traversal, concurrent cycle prevention, and territory-safe cross-boundary continuity. | Waterway tree; duplicate edge table; geometry-derived topology; forced same-territory connectivity. | Graph direction is explicit; boundary reads redact foreign endpoint identity; calculations use a separately authorized canonical graph repository; physical recirculation needs separately governed future modeling. | Accepted |
| D-007 | 2026-08-23 | Make privileged mutations and their append-only audit event one transaction, and permit role administration only by a strictly higher-ranked, in-scope role. See ADR-0005. | A partial grant change or unaudited privilege change is unacceptable; conservative delegation prevents self/peer escalation before official IAM governance is available. | Best-effort audit after mutation; mutable audit rows; same-rank delegation; deletion of cancelled schedules. | Peer/system-admin provisioning requires a higher authority outside this API; scheduled grants are explicitly cancelled without deleting their original validity window; audit reads remain territory-authorized. | Accepted |
| D-008 | 2026-08-23 | Treat raw telemetry as unreliable until a governed validation revision, and separate `telemetry:correct` from operational ingestion authority. | Device claims must not become accounting truth before validation, and district/basin ingestion authority must not imply expert correction authority. | Treat adapter `valid` as immediately usable; reuse `telemetry:write` for corrections; mutate raw rows after validation. | Only valid automatic/expert/corrected revisions are usable by default; raw and estimated data remain visible but unreliable; hydrologists and senior in-scope administrators may create audited human revisions; P2-003 supplies validation rules/transitions. | Accepted |
| D-009 | 2026-08-24 | Govern automatic validation through immutable, distinct-author-approved, effective-dated sensor profile versions; defer rather than append when policy or trusted context is insufficient. See ADR-0006. | Automatic trust must be reproducible, scoped, auditable, and unable to invent universal hydrological policy. | Mutable/default profiles; trust raw predecessors; validate on receipt time; silently accept partial rule evaluation. | Only approved observation-time profiles can append automatic revisions; exact bounded bootstrap is explicit; prior rate/frozen context must already be governed-valid; missing interval policy remains unconfigured. | Accepted |
| D-010 | 2026-08-24 | Persist device-health facts, current projection, and a bounded resumable SSE journal in PostgreSQL; keep communication, numerical trust, and explicit fault state independent. See ADR-0007. | Restart-safe live delivery and historical territory authorization require durable post-commit cursors without inventing device policy or adding a broker to the local MVP. | Process-local events; WebSockets; initial MQTT/Kafka broker; read-time-only derivation. | Slow clients reconnect through bounded journal batches; occurrence-time territory filters precede limits; official cadence/status/power policy remains unconfigured; later multi-instance fan-out may consume the same journal/outbox boundary. | Accepted |
| D-011 | 2026-08-24 | Persist immutable bitemporal condition-signal evaluations from strict governed rules, while keeping alarm severity/catalog and incident workflow in later boundaries. See ADR-0008. | Persistence and hysteresis must survive restart and remain reproducible after late/corrected evidence without accepting caller-authored measurements or silently rewriting operator history. | Mutable current-only state; process timers; generic expressions; caller-supplied values; immediate balance alarms; combined rule/alarm/incident model. | Evaluation resolves configured governed sources at exact effective/known times, appends idempotent evidence, and yields only inactive/pending/active/clearing/deferred condition state; balance remains ineligible until governed residual/uncertainty policy exists. | Accepted |
| D-012 | 2026-08-24 | Permit explicit service-only, synthetic-only historical plan and tolerance approval options for repeatable governed analytics seed data; keep ordinary/API approval non-retroactive. See ADR-0009. | A fresh seed cannot create an elapsed calendar-window deviation through P3 without either future evidence or forged lifecycle SQL; real approval and audit knowledge time must remain truthful. | Backdated raw SQL; future observations; dashboard-fixture accounting; unconfigured-only analytics; general retroactive approval. | The seed reuses normal transactions, distinct actors, audit, immutability, and bitemporal reads; the exceptions are unavailable to HTTP callers and cannot apply to official records. | Accepted |
| D-013 | 2026-08-24 | Authenticate before parsing every protected API request, make local header identity impossible in production, and treat bounded request IDs only as correlation context. See ADR-0010. | Uniform fail-closed behavior removes anonymous validation/existence oracles and prevents a local demo adapter from becoming a production credential. | Per-route ordering; validation before identity; trusting browser roles/request IDs; shipping an embedded production IdP. | Anonymous protected calls consistently return 401 before input parsing or lookup; authenticated validation stays 400; production must inject an accredited MFA-ready provider and ingress controls. | Accepted |
| D-014 | 2026-08-24 | Expose truthful aggregate operational signals and keep local recovery evidence separate from production service objectives. See ADR-0011. | Missing/degraded evidence must never look normal, source and receipt lag have different meanings, and a local archive cannot establish government HA/DR. | Identifier-heavy metrics; zeroes on dependency failure; inferred offline/no-data; volume-only backup; local timing as a production SLO. | Metrics use fixed states and fail closed; process counters require external retention; restore refuses stale schemas and exact-target conflicts; production monitoring/RPO/RTO remain external. | Accepted |
| D-015 | 2026-09-04 | Permit only an explicit production-mode, read-only synthetic public demo with a fixed server-owned auditor identity and a global safe-method boundary. See ADR-0012. | Publishing the local header identity would expose predictable privileged users, while ordinary production mode has no accredited IdP and therefore fails closed. | Public local/Vite proxy; signed-out static deployment; temporary real OIDC; cross-origin static/API split. | Public reviewers can inspect synthetic territory-scoped evidence, but all mutations are blocked; this does not close government IAM, ingress, accreditation, monitoring, backup, or HA/DR blockers. | Accepted |

Add new rows and create an ADR in `docs/adr/` for decisions with meaningful technical trade-offs.

Last reviewed during P7-009 on 2026-09-04. D-015/ADR-0012 adds a separate read-only synthetic public-demo boundary without weakening D-013: local header identity remains impossible in production, client identity headers are ignored, only a fixed seeded auditor is used, and every unsafe HTTP method is rejected before route identity resolution. The deployment adapter serves the compiled web application and API from one origin and does not close B-007/B-008 or add an OT-control path.
