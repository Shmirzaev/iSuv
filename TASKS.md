# Autonomous MVP Task Graph

Status: `[ ]` todo, `[~]` in progress, `[x]` verified, `[!]` blocked.

The primary agent may split tasks further. It must preserve IDs/dependencies, add measurable exit evidence, and keep only one active owner per overlapping file area.

## Phase 0 — Foundation

- [x] **P0-001 — Repository audit and baseline**
  Depends: none.
  Exit: existing stack/features/tests are documented; Git baseline and risks recorded; no destructive rewrite started.
  Evidence: `docs/BASELINE.md`; baseline commit `409668f`; read-only architecture and hydrology audits; Node/pnpm/Docker toolchain probes.

- [x] **P0-002 — Architecture ADRs and module boundaries**
  Depends: P0-001.
  Exit: stack/deployment/data/API decisions are recorded with trade-offs and preserve useful existing code.
  Evidence: accepted ADR-0001 through ADR-0003; ESLint import-boundary rules enforce shared-package and application dependency direction.

- [x] **P0-003 — Repeatable local environment**
  Depends: P0-002.
  Exit: documented startup, `.env.example`, persistent services, health checks, migrations, and seed command work from a clean checkout.
  Evidence: Compose config passes; PostGIS 17/PostGIS 3.5 reports healthy; migration twice and seed twice succeed; API live/readiness/metrics return 200 against the database; Vite shell smoke returns 200; startup and local backup/restore commands are documented.

- [x] **P0-004 — Verification pipeline**
  Depends: P0-003.
  Exit: format/lint/type/test/build commands and CI or one local verification script pass.
  Evidence: clean frozen dependency install and `pnpm verify` pass; CI runs the same verification plus repeated PostGIS migration/seed; production API/web builds pass; dependency audit reports no known vulnerabilities.

## Phase 1 — Identity, topology, and master data

- [x] **P1-001 — Organizations, territories, roles, and authorization skeleton**
  Depends: P0-003.
  Exit: core roles and territory scope are modeled; cross-territory denial tests exist.
  Evidence: migration-backed organizations, hierarchical territories, eight core roles, effective-dated grants, and fail-closed local identity adapter; unit/API tests cover same/ancestor/cross-territory and read-only auditor behavior; real PostGIS tests cover organization isolation, inactive/unknown targets, UTC boundaries, non-overlapping re-grants, and concurrency-safe cycle prevention; full verification and security/reviewer gates pass.

- [x] **P1-002 — Water-network and asset schema**
  Depends: P0-002, P0-003.
  Exit: region/basin/waterway/junction/section/control/station/device/sensor entities, migrations, validation, and APIs exist.
  Evidence: accepted ADR-0004; typed PostGIS master-data migration and territory-authorized read APIs; single authoritative section DAG supports branches/merges and rejects direct/concurrent cycles; cross-boundary identifiers are redacted; effective-dated device installations preserve provenance; DB/contracts enforce entity geometry and WGS84 bounds plus stage→m, discharge→m³/s, accumulated volume→m³; 16 API/unit and 7 real-DB tests pass; QA and reviewer approve.

- [x] **P1-003 — Synthetic 83-hotspot topology seed**
  Depends: P1-002.
  Exit: 83 labeled synthetic top-level nodes with realistic branches and station/device coverage load reproducibly; topology validation passes.
  Evidence: atomic deterministic seed creates exactly 83 labeled entry roots across five compact synthetic basin DAGs with pairwise merge collectors and five outlets; 493 junctions, 571 sections, 83 monitoring-only control structures/stations/devices/installations, and 249 canonical sensors load reproducibly; PostGIS tests prove containment, endpoint alignment, root-to-outlet reachability, cross-territory continuity, classification, and persistent-state reconciliation; seed x2, 17 unit/API tests, and 8 DB tests pass; hydrology, QA, and reviewer gates approve.

- [x] **P1-004 — Audit foundation**
  Depends: P1-001.
  Exit: privileged changes create searchable old/new/reason/actor/time audit records.
  Evidence: accepted ADR-0005; typed v1 role-grant create/revoke/cancel APIs write the grant change and append-only audit event atomically with actor and target organizations, territory, old/new state, reason, request ID, provenance, classification, and UTC time; conservative strictly-higher role delegation prevents self/peer escalation; explicit DB-authoritative scheduled cancellation preserves history and cannot cross the effective boundary; scoped audit reads use bounded filters and composite keyset cursors; 22 API/unit and 12 real-DB tests pass; QA/security and final reviewer approve.

## Phase 2 — Telemetry and data quality

- [x] **P2-001 — Observation schema and revision model**
  Depends: P1-002.  
  Exit: explicit quantity/unit, timestamps, quality, provenance, raw/validated/corrected states, and immutable revision history exist.
  Required evidence: database/contracts reject cross-quantity units and preserve decimal values; server receipt time is distinct from device observation time; observation-time installation resolution snapshots station/device/sensor provenance; concurrent source duplicates are idempotent; revisions are append-only and linear; correction leaves the source revision intact and atomically writes audit evidence; current/as-of/history reads are territory-authorized; no-data and unreliable observations cannot be consumed as normal; accumulated-volume readings remain totalizer counters rather than derived interval delivery.
  Evidence: migration-backed immutable lineages and linear revisions preserve exact decimal values and microsecond UTC observation/receipt instants; contracts and DB enforce stage→m, discharge→m³/s, accumulated_volume→m³, raw-quality, method, uncertainty, classification, and totalizer invariants; ingestion resolves and transactionally rechecks the installation/territory effective at observation time; source identity is vendor-neutral and concurrently idempotent; correction/rejection/estimation require `telemetry:correct`, preserve source evidence, and atomically append audit; current/as-of/history use exact keyset boundaries; 27 API/unit and 15 real-DB tests pass; hydrology, QA/security, and reviewer approve.

- [x] **P2-002 — Telemetry adapter and simulator**
  Depends: P2-001, P1-003.  
  Exit: normal/over/under/offline/stale/spike/frozen/fault scenarios stream through an adapter into storage.
  Required evidence: deterministic scenario generation preserves quantity/unit and accumulated-counter semantics; all 83 synthetic devices/sensors are covered; offline is absence rather than zero; stale/frozen/spike/fault facts remain raw and visibly unreliable pending P2-003 validation; adapter source IDs make replay idempotent; a bounded edge-buffer/replay boundary is represented; no simulator path is enabled as production or exposes physical control.
  Evidence: versioned deterministic synthetic envelopes cover 83 devices and all 249 canonical stage/discharge/accumulated-volume sensors with microsecond-preserving UTC source identities; independent normal/high/low/frozen/spike profiles, explicit totalizer reset/rollover, raw unknown/suspect/invalid trust, and status-only offline gaps retain the P2-003/P2-004 boundaries; a vendor-neutral adapter and bounded append/ack/replay queue preserve timestamps and IDs while reporting overflow/failure; authenticated preview/run routes authorize every derived territory, bind the expected installation territory on each write, are opt-in outside production, and expose no control path; 36 API/unit tests, 5 domain tests, 2 contract tests, and 17 real-DB tests pass with all-249 ingestion/replay/conflict evidence; hydrology, QA/security, and reviewer approve.

- [x] **P2-003 — Idempotency, late/out-of-order, stale, and validation rules**
  Depends: P2-002.  
  Exit: deterministic tests prove duplicate, late, impossible, missing, frozen, and corrected data behavior.
  Required evidence: a versioned, effective-dated validation profile is selected by observation time; absent or insufficient policy defers validation rather than declaring data valid; automatic validation appends one audited linear revision under retry/concurrency and preserves source evidence; late/stale/frozen/counter-transition handling is deterministic and never invents missing readings, interval volume, rating-curve discharge, allocation status, balance, or alarms; coverage distinguishes unconfigured/no-data/incomplete/complete; only valid governed revisions become usable and synthetic validation remains visibly non-authoritative.
  Evidence: immutable organization/territory/sensor/classification-scoped profile versions require explicit rules, distinct-author approval, non-overlapping effective intervals, and observation-time selection; exact microsecond/rational evaluation distinguishes stale, late, out-of-order, frozen, bounded plausibility/rate, bootstrap, and accumulated-counter transitions; absent profiles, temporal-only passes, insufficient governed context, and non-raw current revisions defer without appending; automatic validation preserves source fields, uses governed-valid history across relocation, appends one lineage-locked revision/execution/sanitized audit under retry/correction concurrency, and never creates coverage, interval volume, rating conversion, allocation, balance, alarm, or device-health claims; cross-organization governance is limited to active effective system authority while national/ordinary actors remain organization-local; clean migration/seed repeat, 38 API tests, 9 domain tests, 3 contract tests, and 21 real-DB tests pass; hydrology, QA/security, and reviewer approve.

- [x] **P2-004 — Device health and live delivery API**
  Depends: P2-002.  
  Exit: last seen, data age, power/signal placeholders, quality, and recent history are available to the UI with live updates.
  Required evidence: last-seen receipt time and last-observed source time remain distinct; offline/no-data/stale/unreliable/device-fault states are explicit and never inferred as zero or normal; power/signal fields distinguish unknown placeholders from measured values; territory-authorized current/history APIs and resumable bounded SSE update clients without a page reload; synthetic status events remain labeled and cannot create physical-control paths; reconnect, slow-consumer, authorization, and degraded-database tests pass.
  Evidence: accepted observations and explicit status-only events transactionally append immutable health facts, update an independently modeled current projection, and publish durable bigint journal cursors; revision-one receipt and numerical source time remain distinct through correction/validation and immutable installation provenance survives relocation; connection, data condition, and explicit fault state cannot clear or normalize one another; no cadence invents current/no-data, while stale/unreliable/no-data/unconfigured remain typed; power/signal distinguish unknown from measured zero; synthetic classification cannot be upgraded; occurrence-territory authorization is applied before keyset history and bounded 250-event reconnecting SSE limits, with scoped cursor reset/snapshot, empty heartbeat, strict cursor validation, and database-degraded 503 behavior; concurrent replay/collision, equal-time fault priority, relocation, same-microsecond cursors, raw migration replay, and no-control tests pass; `pnpm verify` passes (40 API, 9 domain, 3 contract tests), full DB tests pass 25/25, and hydrology, QA/security, and reviewer approve.

## Phase 3 — Allocation and accounting

- [x] **P3-001 — Versioned allocation plans and approvals**
  Depends: P1-001, P1-002, P1-004.  
  Exit: effective-dated plan versions, requester/approver/reason/reference, and historical lookup are implemented and tested.
  Required evidence: immutable plan versions are scoped to organization/territory and an explicit delivery interval/quantity unit; draft/request/approve/supersede transitions require authorized distinct actors and effective non-overlap; changes preserve reason, legal/reference metadata, requester, approver, approval time, and audit event in one transaction; absent official plans remain visibly synthetic and cannot be used as official compliance; current/as-of/history reads and mutations enforce role plus territory with concurrency and non-enumeration tests.
  Evidence: one immutable plan identity per active water section carries synthetic-only authority and immutable versions/entries represent exact half-open whole-interval targets in m³ without discharge, stage, actual-delivery, tolerance, or physical-control claims; database-authorized draft/request/approve/supersede transitions use active effective role-plus-territory grants, distinct actors, database-authored times, advisory serialization, and deferred non-overlap while exact known-time projection preserves later-discovered supersession; plan, version, and entry rows carry actor/reason/request provenance and atomically emit database-owned append-only audits containing each planned-volume commitment; schedule gaps remain distinct from explicit zero and from no approved plan; contracts expose declared versus governed validity, synthetic non-authority, opaque history cursors, and typed unavailable states; API routes fail closed with non-enumerating role/territory authorization. Fresh migrations and repeat seed pass; `pnpm verify` passes (45 API, 11 domain, 4 contract tests); deterministic full DB tests pass 27/27 including raw-DML bypass, bitemporal microseconds, concurrency, rollback, and audit completeness; dependency audit is clean; hydrology, QA/security, and final reviewer approve.

- [x] **P3-002 — Quantity conversion and volume integration boundary**
  Depends: P2-001.  
  Exit: stage/discharge/volume remain distinct; synthetic rating-curve adapter and discharge-to-volume integration are tested.
  Required evidence: conversion accepts only usable stage in metres plus an explicit effective version of a synthetic site-specific model and produces separately typed discharge in m³/s with complete source/model provenance; absent, out-of-range, ambiguous, or untrusted inputs defer instead of producing flow; discharge integration uses exact UTC interval arithmetic, bounded-gap rules, and explicit complete/no-data/incomplete/unreliable coverage without treating an instantaneous sample as volume; accumulated-volume inputs remain counters and only an exact governed delta/rollover calculation can become interval m³ while resets or unknown transitions defer; decimal and microsecond boundary, mixed-unit, gap, ordering, duplicate-time, reset/rollover, and synthetic non-authority tests pass; no official hydrological accuracy, allocation comparison, alarm, balance, or physical-control claim is introduced.
  Evidence: immutable synthetic-only rating curves and integration policies are site, sensor, installation, organization, and territory scoped; distinct authorized request/approval actors, database-owned times, append order, non-overlap, immutable rows, and atomic audit are enforced beneath the API. The read API requires an explicit sensor, selects effective models and governed observation revisions at exact `knownAt`, pins observations to the policy installation, and returns an explicitly typed interval volume in m³ with rational precision, coverage, source revision lineage, curve/policy versions, quality, provenance, and non-authority flags. Exact trapezoidal integration supports direct valid discharge; stage in metres uses a bounded synthetic piecewise-linear, stationary/no-hysteresis curve and remains estimated; an approved synthetic counter policy permits only an exact, monotonic, bounded-gap delta between normal accumulated-volume readings. Missing policy/curve/endpoints, gaps, unusable revisions, mixed streams, out-of-range stage, negative discharge, counter decrease, reset, rollover, or unknown transitions defer without a fabricated value. Fresh migrations 0001–0009, repeat seed, and raw 0009 replay twice pass; `pnpm verify` passes (47 API, 14 domain, 5 contract tests); deterministic full DB tests pass 29/29 including exact direct/stage/counter integration and historical revision selection; hydrology, QA/security, and final reviewer approve.

- [x] **P3-003 — Planned-vs-actual and configurable tolerances**
  Depends: P3-001, P3-002.  
  Exit: interval-aligned absolute/percentage variance and scoped tolerances are tested.
  Required evidence: every plan entry has one immutable governed station/sensor/installation/derivation-method binding before plan approval; comparison requires an exact whole-entry interval and complete valid actual interval volume without client-selected meter switching or proration; effective-dated synthetic tolerance versions are role-plus-territory governed, distinct-author approved, non-overlapping, bitemporally reproducible, and atomically audited; signed and absolute m³ variance plus percentage variance use exact arithmetic with explicit planned-zero behavior and asymmetric under/over thresholds; missing plan/schedule/binding/tolerance, incomplete or unreliable actuals, and estimated stage-derived actuals defer rather than appear within tolerance; outputs remain synthetic decision support, never official compliance, alarms, balance, travel-time, or physical control.
  Evidence: authenticated, territory-scoped APIs create immutable plan-entry measurement bindings, create synthetic section policies, request bounded tolerance versions, and approve them only through a separate caller; database triggers independently enforce topology/reference-plane and sensor/unit/installation scope, draft-only binding, complete binding before plan request, sequential requested-to-approved lifecycle, distinct active scoped actors, database-owned microsecond timestamps, bounded non-overlap, immutability, and atomic request/approval audit old/new state. Read-time deviation resolution selects the exact approved plan entry, binding, tolerance, derived interval volume, and observation revisions at one `knownAt`; exact rational signed/absolute m³ and percentage variance preserve offset-equivalent instants and explicit planned-zero semantics. Missing governance, schedule gaps, partial intervals, incomplete/unreliable actuals, and estimated stage results remain typed unassessable outcomes. Fresh migrations 0001–0010, repeat seed, and raw 0010 replay twice pass; `pnpm verify` passes (50 API, 17 domain, 7 contract tests); the clean serial DB suite passes 30/30, dependency audit is clean, Compose/diff checks pass, built API live/ready/metrics return 200 with request IDs, and QA/security plus final reviewer approve.

- [x] **P3-004 — Parent-child water balance and travel-time model**
  Depends: P1-003, P3-002.  
  Exit: expected terms and unexplained residual are calculated; time alignment is configurable/explicit and tested.
  Required evidence: one approved synthetic model version snapshots every immediate canonical section incident to a junction exactly once as incoming or outgoing, with an immutable governed station/sensor/installation/method/reference-plane binding and fixed nonnegative travel time; source intervals are shifted exactly in microseconds toward the junction accounting interval without changing duration, interpolation, geometry inference, recursive aggregation, or client-selected meters; exact m³ inflow, outflow, signed storage change, explicit known additions/removals, and unexplained residual use rational arithmetic only when every required component and explicit-zero-capable assumption entry exactly covers the requested interval at one `knownAt`; missing topology/binding/lag/assumption, partial or unreliable data, and estimated stage-derived volume defer instead of becoming a residual; model request/approval is bounded, non-overlapping, distinct-author, role-plus-every-participating-territory governed, immutable, bitemporally reproducible, and atomically audited; results preserve component/derivation/revision/model provenance and remain synthetic, nonofficial, non-alarm, non-incident, and non-control decision support.
  Evidence: a synthetic junction model and immutable requested/approved versions snapshot every immediate canonical incoming/outgoing section with explicit station/sensor/installation/method/reference plane, fixed travel microseconds capped at one year, and exact interval assumptions; database triggers enforce canonical completeness, reference-plane station/control-structure attachment, installation coverage across shifted windows, method/unit compatibility, sequential bounded versions, non-overlapping approval, distinct active actors with every participating territory, immutable details, null requested-state approval evidence, and database-owned audit for model/version/component/assumption changes. Read-time calculation selects the approved model and governed observation revisions at one exact `knownAt`, shifts all four plane/direction cases without changing duration, accepts only complete valid direct-discharge or counter volumes, and computes exact rational incoming, outgoing, signed storage, known additions/removals, and residual; absent models/assumptions or any ineligible component return typed deferral. Results remain synthetic, nonofficial, non-alarm, and non-control. Fresh migrations 0001–0011, repeat seed, and raw 0011 replay twice pass; `pnpm verify` passes (54 API, 20 domain, 9 contract tests); the clean serial DB suite passes 31/31 including exact computed balance, raw-DML audit, forged approval rejection, immutability, and distinct approval; dependency audit is clean, Compose/diff checks pass, built API live/ready/metrics return 200 with request IDs, and QA/security plus final reviewer approve.

## Phase 4 — Alarms and incidents

- [x] **P4-001 — Rule engine**
  Depends: P2-003, P3-003, P3-004.  
  Exit: rules use validity, tolerance, persistence, rate of change, and hysteresis; single bad spikes do not create critical alerts.
  Required evidence: immutable synthetic rule identities and bounded effective-dated versions are sequential, non-overlapping, distinct-author approved, role-plus-territory governed, and atomically audited; a strict observation-threshold rule binds one sensor and exact quantity/unit with high/low enter and clear thresholds, positive activation/clear persistence, a maximum evidence gap, explicit conservative uncertainty, and an optional exact same-quantity rate gate, while an allocation-deviation rule consumes P3-003 whole-entry computed condition/tolerance instead of reimplementing it; evaluation accepts only configured subjects and exact `effectiveAt`/`knownAt`, resolves governed source revisions internally, orders evidence by event time, and persists immutable idempotent evidence/run snapshots plus a rebuildable current projection; exact rational and microsecond logic distinguishes inactive, pending activation, active, pending clear, and deferred, requires at least two contiguous qualified facts, treats threshold equality as non-breach, applies symmetric high/low hysteresis and independent clear persistence, and never bridges configured gaps; missing, raw, suspect, invalid, estimated, wrong-unit, unknown-uncertainty, unassessable, late/out-of-order, or corrected evidence is visible and cannot activate or falsely clear a condition; later cutoffs append rather than rewrite earlier operational knowledge; balance remains explicitly ineligible while P3-004 returns `alarmEligible: false`; outputs contain no severity, alarm/incident lifecycle, notification, or physical-control action, which remain P4-002/P4-003 boundaries.
  Evidence: immutable synthetic-only rule identities and strict, bounded observation/allocation rule versions are database-governed by sequential request, distinct scoped approval, non-overlap, actor/request/reason provenance, and atomic audit; exact rational/microsecond evaluation enforces strict high/low threshold boundaries, uncertainty-safe entry/clear hysteresis, independent persistence, at least two facts, maximum gaps, optional same-quantity rate gates, and P3-003 whole-entry deviation reuse without duplicating tolerance math. Governed event-time evidence is selected at exact `knownAt`; missing policy, missing facts, raw/invalid/untrusted/estimated evidence, wrong bindings, uncertainty, gaps, and unassessable allocation remain typed deferred and cannot activate or falsely clear. Configured and unconfigured cutoffs append immutable idempotent snapshots; late invalid evidence preserves the earlier active run while the later current projection becomes deferred; the projection validates run identity and is deterministically rebuildable. Fresh migrations 0001–0012, repeat seed, and raw 0012 replay x2 pass; `pnpm verify` passes (59 API, 26 domain, 13 contract tests); the full serial DB suite passes 32/32 including lifecycle bypass, forged result/projection rejection, audit, replay, correction history, and synthetic non-authority; QA/security and final reviewer approve.

- [x] **P4-002 — Alarm severity and event catalog**
  Depends: P4-001.  
  Exit: required water and device event classes, separate water condition/severity, and evidence fields exist.
  Required evidence: a synthetic governed catalog explicitly enumerates over/under allocation, unexplained balance, sudden flow change, high stage, dry canal, sensor frozen, sensor impossible, communication loss, power problem, calibration overdue, and network inconsistency; immutable bounded catalog and rule-binding versions are sequential, distinct-author approved, non-overlapping, role-plus-territory scoped, effective/known-time reproducible, and atomically audited; event type, water condition, system/device condition, severity, confidence/eligibility, and synthetic non-authority remain independent typed fields; catalog entries whose source bridge is unavailable remain explicitly unconfigured rather than normal; only compatible active P4-001 signal runs can materialize one idempotent alarm activation episode, while pending activation creates nothing, pending clear preserves an existing alarm, inactive after governed clear persistence records automatic clear evidence, and deferred/unreliable/late-corrected evidence neither creates nor falsely clears; alarm snapshots retain exact catalog/rule/version/signal/evidence/provenance/effective/known/detected references and never rewrite severity or condition after policy changes; unsupported balance and device sources cannot bypass P3-004 `alarmEligible: false`, unconfigured cadence, or unknown power/signal; outputs and APIs contain no acknowledgement, assignment, comments, resolution, incident, escalation execution, notification, or physical-control action, which remain P4-003 boundaries.
  Evidence: the canonical synthetic catalog contains exactly the twelve required event families and separates event type, water condition, device/system condition, and severity; immutable catalog versions bind only compatible approved P4-001 rule versions and are sequential, bounded, distinct-author approved, role-plus-territory governed, bitemporally selected, and atomically audited. Materialization resolves the approved rule signal internally under a per-rule transaction lock, creates one idempotent active episode, preserves pending/deferred evidence without false clear, automatically clears only after governed inactive persistence, and permits later recurrence while retaining immutable catalog/rule/signal/evidence snapshots. Database triggers reject expired or mismatched catalog/rule versions, forged cross-version evidence, cancelled grants, direct snapshot mutation, and unsupported/unconfigured bridges. Fresh migrations 0001–0013, repeat seed, and raw 0013 replay twice pass; `pnpm verify` passes (62 API, 31 domain, 18 contract tests); the clean serial DB suite passes 33/33; production dependency audit is clean; Compose and diff checks pass; the built API live/ready/metrics endpoints return 200 with request IDs; QA/security and final reviewer approve.

- [x] **P4-003 — Incident lifecycle and escalation metadata**
  Depends: P4-002, P1-001, P1-004.  
  Exit: acknowledge/assign/comment/resolve/close timeline, ownership, metrics, and audit are implemented.
  Required evidence: an incident is a synthetic, territory-scoped operator case linked immutably to one or more same-territory P4-002 alarm episodes, with each alarm belonging to at most one incident and concurrent creation/linking unable to duplicate ownership; automatic alarm active/cleared state, snapshotted severity/conditions/evidence, and human incident lifecycle remain separate, so deferred/unreliable evidence never looks normal, automatic clear never closes a case, resolution requires every linked alarm to be automatically cleared, and closure requires prior resolution; authenticated incident writers can create, acknowledge, investigate, assign an active same-organization/in-scope operator, comment, record corrective action, resolve, and close through database-enforced transitions while closed cases reject mutation; every action has database-owned UTC time, actor, reason/request provenance, an immutable sequential timeline, atomic searchable audit, and role-plus-territory nonenumerating API behavior; effective-dated, distinct-author-approved synthetic escalation-policy versions snapshot procedure/tier and exact acknowledgement/resolution targets at incident creation, while an explicit read cutoff computes unconfigured/pending/overdue/met response metrics without sending notifications, altering severity/state, creating work orders, or controlling infrastructure; policy changes never rewrite existing incidents, missing policy is visibly unconfigured, and all records remain synthetic/nonofficial under B-006.
  Evidence: governed synthetic escalation-policy identities and bounded versions are sequential, distinct-author approved, non-overlapping, role-plus-territory scoped, atomically audited, and snapshotted at incident creation. Same-territory alarm linking is immutable and concurrency-safe with one incident owner per alarm; automatic alarm state remains separate from the database-enforced open/acknowledged/investigating/resolved/closed operator lifecycle, all linked alarms must clear before resolution, and closed cases reject mutation. Assignment, comments, corrective actions, and lifecycle changes append an actor/reason/request-attributed UTC timeline and searchable audit; raw-DML guards reject fabricated or duplicate lifecycle/link events. Exact-cutoff acknowledgement/resolution metrics remain synthetic metadata only and policy succession cannot rewrite a case. Fresh migrations 0001–0014, repeat seed, and raw 0014 replay twice pass; `pnpm verify` passes (66 API, 34 domain, 19 contract tests); the clean serial DB suite passes 34/34; production dependency audit is clean; Compose and diff checks pass; built API live/ready/metrics endpoints return 200; QA/security and final reviewer approve.

## Phase 5 — Operator application

- [x] **P5-001 — Application shell, navigation, localization, accessibility baseline**
  Depends: P0-003, P1-001.  
  Exit: role-aware shell uses the typed current-session boundary without fabricating operational data; global alarm/incident access is keyboard reachable only for session-derived alarm/incident readers, and Audit is discoverable only for the corresponding role set. All shell strings are available in Uzbek, Russian, and English; an unambiguous synthetic/nonofficial banner, semantic landmarks, skip link, visible focus treatment, responsive navigation, and label/icon/value status patterns exist. Identity, unauthenticated, unavailable, and territory-scope states are explicit; no direct physical-control path is present. Required evidence: rendered-markup/model tests prove complete translation coverage, native keyboard links/radios, skip target, semantic landmarks, current navigation, global alarm link display gating, and fail-closed identity presentation; web format, lint, type, test, build, and browser keyboard smoke checks pass.
  Evidence: the shell parses only the typed current-session response, fails closed when identity is absent/unavailable, exposes alarm/incident and audit navigation only from conservative session-derived reader roles, and keeps API authorization authoritative. Shared typed i18n provides every shell, role, scope, measurement, and status string in Uzbek, Russian, and English; the static and synchronous first-render language is Uzbek, switching persists, and a blocked storage backend cannot prevent rendering. The synthetic/nonofficial notice, monitoring-only boundary, exact stage m/discharge m³/s/volume m³ labels, and five structured status examples remain visible without inventing operational values. Semantic header/nav/main/footer landmarks, native links/radios, current-page state, skip target with visible focus, text/icon/value statuses, reduced-motion handling, and responsive navigation are covered by six web tests. `pnpm verify` passes (66 API, 34 domain, 19 contract, 6 web tests and both production builds); dependency audit, Compose, no-control, no-bundled-local-identity, and diff checks pass. Authenticated browser smoke resolves the seeded system-admin role/scope, locale and global alarm navigation; 390 px smoke has no horizontal overflow or console warnings. QA/security and final reviewer approve after status-structure and initial-language fixes.

- [ ] **P5-002 — Command dashboard**  
  Depends: P2-004, P3-003, P3-004, P4-002, P5-001.  
  Exit: required KPIs, ranked deviations, plan/actual/previous comparison, confidence, period controls, and drill-down work.

- [ ] **P5-003 — Live operations table and inspector**  
  Depends: P2-004, P3-003, P5-001.  
  Exit: required columns, filters, live/stale behavior, and detail inspector work with seeded data.

- [ ] **P5-004 — GIS plus hydrological network view**  
  Depends: P1-003, P2-004, P3-004, P5-001.  
  Exit: aggregation/progressive detail, topology view, drill panel, upstream/downstream tracing, accessible states, and temporal playback abstraction work.

- [ ] **P5-005 — Alarm and incident center**  
  Depends: P4-003, P5-001.  
  Exit: queue, filters, acknowledgement, assignment, comments, timeline, and closure work under territory authorization.

## Phase 6 — Analytics, reports, and audit

- [ ] **P6-001 — Delivery, deviation, balance, quality, and availability analytics**  
  Depends: P3-004, P5-001.  
  Exit: required analyses and period/scope filters work and do not overstate data confidence.

- [ ] **P6-002 — Versioned report snapshots and exports**  
  Depends: P3-001, P4-003, P6-001.  
  Exit: required MVP reports include metadata, are reproducible, and support print/PDF plus tabular export.

- [ ] **P6-003 — Audit explorer**  
  Depends: P1-004, P5-001.  
  Exit: authorized users can search and inspect important changes without mutating them.

## Phase 7 — Hardening and final acceptance

- [ ] **P7-001 — End-to-end operational scenarios**  
  Depends: all P1–P6 MVP tasks.  
  Exit: automated/browser scenarios cover normal delivery, over/under allocation, device outage, invalid spike, unexplained balance, acknowledgement, resolution, and report generation.

- [ ] **P7-002 — Security and authorization review**  
  Depends: P7-001.  
  Exit: no critical finding; territory/role tests pass; no physical control path; secrets and dependency checks pass.

- [ ] **P7-003 — Performance, observability, backup/restore, and degraded-mode smoke tests**  
  Depends: P7-001.  
  Exit: agreed synthetic load works; telemetry lag/errors are observable; backup/restore and outage behavior are demonstrated/documented.

- [ ] **P7-004 — Acceptance audit and handover**  
  Depends: P7-002, P7-003.  
  Exit: every `MVP REQUIRED` criterion has linked evidence; README/operator/dev docs are current; external rollout blockers are explicit; final demo starts from a clean checkout.
